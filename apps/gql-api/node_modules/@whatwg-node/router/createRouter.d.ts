import type { Router, RouterBaseObject } from './types';
interface RouterOptions<TServerContext = {}> {
    base?: string;
    RequestCtor?: typeof Request;
    plugins?: Array<(router: RouterBaseObject<TServerContext>) => RouterBaseObject<TServerContext>>;
}
export declare function createRouter<TServerContext = {}>(options?: RouterOptions<TServerContext>): Router<TServerContext>;
export {};
